###############################################
# MediaMTX — RTSP hub + WebRTC/HLS output
###############################################

rtspTransports: [udp, tcp]

# WebRTC for low-latency browser playback
webrtc: yes
webrtcAddress: :8889
webrtcLocalUDPAddress: ""
webrtcLocalTCPAddress: :8189
webrtcIPsFromInterfaces: yes
webrtcAdditionalHosts: []

# HLS as fallback
hlsAlwaysRemux: yes
hlsVariant: lowLatency
hlsSegmentCount: 7
hlsSegmentDuration: 500ms

paths:
  # --- RAW camera inputs ---
  raw-cam1:
    runOnInit: >
      ffmpeg -re -stream_loop -1
      -i /videos/fulhadoo/2023-01-23_02-20-02_cam3.mp4
      -an -c:v copy
      -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
    runOnInitRestart: yes

  raw-cam2:
    runOnInit: >
      ffmpeg -re -stream_loop -1
      -i /videos/fulhadoo/2023-01-28_02-00-01_cam3.mp4
      -an -c:v copy
      -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
    runOnInitRestart: yes

  raw-cam3:
    runOnInit: >
      ffmpeg -re -stream_loop -1
      -i /videos/fulhadoo/2023-02-08_01-20-01_cam3.mp4
      -an -c:v copy
      -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
    runOnInitRestart: yes

  raw-cam4:
    runOnInit: >
      ffmpeg -re -stream_loop -1
      -i /videos/fulhadoo/2023-01-23_02-05-02_cam3.mp4
      -an -c:v copy
      -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
    runOnInitRestart: yes

  raw-cam5:
    runOnInit: >
      ffmpeg -re -stream_loop -1
      -i /videos/fulhadoo/2023-02-08_01-15-01_cam3.mp4
      -an -c:v copy
      -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
    runOnInitRestart: yes

  raw-cam6:
    runOnInit: >
      ffmpeg -re -stream_loop -1
      -i /videos/fulhadoo/2023-01-28_01-50-01_cam3.mp4
      -an -c:v copy
      -f rtsp rtsp://localhost:$RTSP_PORT/$MTX_PATH
    runOnInitRestart: yes

  # --- Processed output from DeepStream ---
  cam1:
  cam2:
  cam3:
  cam4:
  cam5:
  cam6:

  # Wildcard: accepts any RTSP publisher on any path
  all_others:
